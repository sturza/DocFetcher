# The internal program settings are stored here.
# Only modify this file if you know what you're doing.
# 
# How to edit this file:
# * The program loads this file on startup and overwrites it
#   on closing. Thus, remember to close the program
#   before editing this file, otherwise all your changes
#   will get overwritten.
# * To restore the default value of an entry, close the
#   program, delete the entry, then restart the program. The
#   program will now use the default value and write it to
#   disk when it closes.
# * Clear or delete the entire file to restore all default
#   values.
#
# Thu Apr 09 22:30:58 CEST 2020

# Boolean entries. Allowed values: true, false
ShowManualOnStartup = true
UseOrOperator = true
HideOnOpen = false
ClearSearchHistoryOnExit = false
UseTypeAheadSearch = false
ResetLocationFilterOnExit = true
HotkeyEnabled = true
ShowFilterPanel = true
ShowPreviewPanel = true
ShowPreviewPanelAtBottom = true
FilesizeFilterMaximized = true
TypesFilterMaximized = false
LocationFilterMaximized = false
MainShellMaximized = false
PreferHtmlPreview = true
HighlightingEnabled = true
ShowPreviewLineNumbers = true
ShowRelativePathsMessage = true
AutoScrollToFirstMatch = true
CloseToTray = false
AllowOnlyOneInstance = false
ShowJavaVersion = false

# Column orderings.
ResultPanelColumnOrder = 

# Comma-separated lists of table column widths.
ResultPanel = 250, 75, 75, 200, 75, 350, 100, 100
IndexingErrorTable = 100, 100, 500
PatternTable = 200, 75, 75

# Font entries, consisting of font name, height and style.
PreviewWindows = Lucida Sans Unicode, 11, 0
PreviewLinux = Sans, 10, 0
PreviewMacOsX = Helvetica, 12, 0
PreviewMonoWindows = Courier New, 10, 0
PreviewMonoLinux = Monospace, 10, 0
PreviewMonoMacOsX = Monaco, 10, 0

# Integer entries.
FilterPanelWidth = 250
LuceneAnalyzer = 0

# Comma-separated lists of integers.
PreviewHighlighting = 255, 255, 0
Hotkey = 262144, 16777233
FilterSash = 1, 1
RightSashHorizontal = 1, 1
RightSashVertical = 1, 1

# Comma-separated lists of sash weights.
ProgressPanel = 2, 1

# Window dimensions: x, y, width and height. If x < 0 or
# y < 0, the window is centered relative to the screen or to
# its parent window.
MainWindow = -1, -1, 800, 600
IndexingDialog = -1, -1, 450, 500
FileExtensionChooser = -1, -1, 300, 450
PreferencesDialog = -1, -1, 500, 400

# String entries. All values allowed, except for \, \t, \n,
# \r, \f, =, :, #, ! and leading whitespace, which must be
# escaped by a preceding \.
LastIndexedFolder = /Users/dev
LastIndexedPSTFile = 

# Semicolon-separated lists of strings.
SearchHistory = 